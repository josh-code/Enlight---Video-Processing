# Required for S3 upload
# Your backend API base URL (no trailing slash). e.g. https://api.yourdomain.com or http://localhost:4000
BACKEND_URL=http://localhost:9000

# Admin JWT from your backend (login as SUPERADMIN, copy token). Do not commit real tokens.
AUTH_TOKEN=token_here

# Defaults
DEFAULT_LANGUAGE=en

# Endpoints (match backend routes; change only if your API uses different paths)
PRESIGN_EXACT_ENDPOINT=/api/v1/admin/content/aws/uploadUrlExact
PRESIGN_BATCH_ENDPOINT=/api/v1/admin/content/aws/uploadUrlExactBatch
PRESIGN_BATCH_SIZE=200
FILE_CREATE_ENDPOINT=/api/v1/admin/content/files
AUTH_VALIDATE_ENDPOINT=/api/v1/admin/auth/verify-admin
